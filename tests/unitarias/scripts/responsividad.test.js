/*import { expect, describe, test } from '@jest/globals';
import { detAbrirMenu, detTamCarga } from '../../../src/utils/Responsividad';

describe("Validar la función 'detAbrirMenu'", () => {
    test("CP - 17", () => {
        const res = detAbrirMenu(false, true, "vertical");
        expect(res).toBe(false);
    });

    test("CP - 18", () => {
        const res = detAbrirMenu(true, true, "vertical");
        expect(res).toBe(false);
    });

    test("CP - 19", () => {
        const res = detAbrirMenu(false, false, "vertical");
        expect(res).toBe(true);
    });

    test("CP - 20", () => {
        const res = detAbrirMenu(false, true, "horizontal");
        expect(res).toBe(true);
    });
});

describe("Validar la función 'detTamCarga'", () => {
    test("CP - 21", () => {
        const res = detTamCarga(true, "vertical", false, 100);
        expect(res).toBe(92);
    });

    test("CP - 22", () => {
        const res = detTamCarga(false, "vertical", false, 100);
        expect(res).toBe(92);
    });

    test("CP - 23", () => {
        const res = detTamCarga(true, "horizontal", false, 100);
        expect(res).toBe(92);
    });

    test("CP - 24", () => {
        const res = detTamCarga(false, "horizontal", true, 500);
        expect(res).toBe(240);
    });
});*/